${{header}}
${{detect-loader}}

if [ "$1" = purge ]; then
    rm -rf /var/lib/${{app_name}}/data
    rm -rf /var/log/${{app_name}}

    userdel bdm >/dev/null 2>&1 || true
    groupdel bdm >/dev/null 2>&1 || true
fi

exit 0
